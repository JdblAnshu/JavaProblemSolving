import java.util.Comparator;

public class SpringBoot {


	/*
	When use iterator over for loop
	Removing Elements During Iteration:
If you need to remove elements from a list while iterating over it, Iterator is the better choice.
This is because Iterator provides a remove() method that allows you to safely remove elements while iterating. With a for-each loop, you cannot directly remove elements from the list during iteration; doing so would throw a ConcurrentModificationException.
List<Integer> numbers = new ArrayList<>();
numbers.add(10);
numbers.add(20);
numbers.add(30);
numbers.add(40);

Iterator<Integer> iterator = numbers.iterator();
while (iterator.hasNext()) {
   System.out.println(iterator.next());
}


////////////////////////////////



	 */
}

//TODO -check for volatile keyword and synchronized keyword
